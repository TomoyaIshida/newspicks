#account-page.account-page
  .account-page__inner.clearfix
    .account-page__inner__top.account-page__header
      %h2 Log in
      - if controller_name != 'sessions'
        = link_to "すでにアカウントをお持ちの方はこちら", new_user_registration(resource_name)
        %br/
    .account-page__inner__bottom.user-form
      = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
        .field
          .field-input
            = f.email_field :email, autofocus: true, placeholder:"登録したメールアドレス"
          .field
            .field-input
              = f.password_field :password, autocomplete: "off", placeholder: "パスワード(8文字以上)"
          .actions
            = f.submit "ログイン", class: 'btn'
    .account-page__inner__text
      上記の項目をすべて入力してください。

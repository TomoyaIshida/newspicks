#account-page.account-page
  .account-page__inner.clearfix
    .account-page__inner__top.account-page__header
      %h2 アカウント登録
      %p 下記の項目をすべて入力してください。
    .account-page__inner__bottom.user-form
      = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
        / = devise_error_messages!
        .field
          .field-input
            = f.text_field :name, autofocus: true, placeholder: "登録する名前"
        .field
          .field-input
            = f.email_field :email, placeholder: "登録するメールアドレス"
        .field
          .field-input
            = f.password_field :password, autocomplete: "off", placeholder: "パスワード(8文字以上)"
        .actions
          = f.submit "はじめる", class: 'btn'
    .account-page__inner__text
      利用開始をもって《利用規約》と《個人情報保護方針》に同意したものとみなします。

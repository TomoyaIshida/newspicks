/ !!!
/ %div.like-link{id: "like-link-#{pick.id}"}
/   - if current_user.likes.find_by(pick_id: pick.id)
/     = link_to "いいね"like_path(pick.id), method: :delete
/       .btn.btn-default
/   - else
/     = link_to likes_path, method: :post
/       .btn.btn-default
